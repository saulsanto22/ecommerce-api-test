{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan vendor:publish --provider=\"L5Swagger\\L5SwaggerServiceProvider\" --force && php artisan l5-swagger:generate && mkdir -p public/vendor/l5-swagger && cp -r resources/views/vendor/l5-swagger/asset/* public/vendor/l5-swagger/ && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckTimeout": 120
    }
}