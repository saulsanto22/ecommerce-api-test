{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan config:clear && php artisan cache:clear && php artisan l5-swagger:generate && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckTimeout": 120
    }
}